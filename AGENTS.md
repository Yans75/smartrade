# Smartrade — agent notes

Independent project, not connected to any third-party editor or platform.
Standard git workflow: feature branches, PRs, no special sync constraints.

See `README.md` for the stack, the signal-generation pipeline, and the
environment variables required to run this locally.
